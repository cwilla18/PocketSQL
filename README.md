# PocketSQL
A CLI that loads CSV/JSON files as in-memory tables and runs a useful subset of SQL against them, built as a real compiler-style pipeline so the learning is in the internals, not a library wrapper.
